{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',
  extends: [
    'config:recommended',
    'docker:enableMajor',
    'helpers:pinGitHubActionDigests',
    'github>uncharted311/home-ops//.renovate/changelogs.json5',
    'github>uncharted311/home-ops//.renovate/groups.json5',
    'github>uncharted311/home-ops//.renovate/labels.json5',
    'github>uncharted311/home-ops//.renovate/semanticCommits.json5',
    ':dependencyDashboard',
    ':semanticCommits',
    ':timezone(Europe/Berlin)',
    ':prConcurrentLimit10',
    ':prHourlyLimit4',
  ],
  dependencyDashboardTitle: 'Renovate Dashboard 🤖',
  assignees: [
    'uncharted311',
  ],
  pinDigests: false,
  ignorePaths: [
    '**/*.sops.*',
    '**/resources/**',
  ],
  enabledManagers: [
    'docker-compose',
    'dockerfile',
    'github-actions',
    'custom.regex',
  ],
  customManagers: [
    {
      customType: 'regex',
      description: 'Traefik plugins',
      managerFilePatterns: [
        '/\\.ya?ml$/',
        '/\\.toml$/',
      ],
      matchStrings: [
        'moduleName:\\s*["\']?github\\.com/(?<depName>[^"\'\\s]+)["\']?\\s*\\n\\s*version:\\s*["\']?(?<currentValue>[^"\'\\s]+)',
      ],
      datasourceTemplate: 'github-releases',
      versioningTemplate: 'semver',
      extractVersionTemplate: '^v?(?<version>.*)$',
    },
  ],
  packageRules: [
    // 1. GLOBALE DOCKER REGELN
    {
      description: 'Docker uses docker versioning by default',
      matchDatasources: [
        'docker',
      ],
      versioning: 'docker',
      pinDigests: false,
    },
    {
      description: 'Disable digest updates for Docker registries',
      matchDatasources: [
        'docker',
      ],
      matchUpdateTypes: [
        'digest',
      ],
      enabled: false,
    },
    {
      description: 'Separate major container upgrades and prioritize them',
      matchDatasources: [
        'docker',
      ],
      separateMajorMinor: true,
      prPriority: 10,
    },
    // 2. SPEZIFISCHE MANAGER REGELN
    {
      description: 'Automerge GitHub Actions digest/patch',
      matchManagers: [
        'github-actions',
      ],
      matchUpdateTypes: [
        'digest',
        'patch',
      ],
      automerge: true,
    },
    // 3. SPEZIFISCHE VERSIONIERUNGEN
    {
      description: 'Special versioning for Linuxserver.io images',
      matchDatasources: [
        'docker',
      ],
      matchPackageNames: [
        '/^linuxserver\\//',
      ],
      matchCurrentValue: '/^version-/',
      versioning: 'regex:^version-(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)(?:\\.(?<build>\\d+))?(?:-ls(?<revision>\\d+))?$',
    },
    {
      description: 'Pangolin weird versioning',
      matchPackageNames: [
        'fosrl/pangolin',
      ],
      versioning: 'regex:^(?<compatibility>.*-)?(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)$',
      pinDigests: false,
    },
  ],
}